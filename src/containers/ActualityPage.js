import React from 'react'
import { StyleSheet, Text, View } from 'react-native';

const ActualityPage = () => {
    return (
        <View style={styles.container}>
            <Text>ActualityPage</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'column',
        margin: 1
    },
})

export default ActualityPage;
